name: Deploy to MQTT Mock Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: SSH Agent のセットアップ
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: known_hosts にホスト追加
        run: |
          ssh-keyscan -H xr-mock.jn.sfc.keio.ac.jp >> ~/.ssh/known_hosts

      - name: サーバーにデプロイ
        run: |
          ssh deploy@xr-mock.jn.sfc.keio.ac.jp << 'EOF'
            cd /home/deploy/sfv-mock-mosquitto-auto
            git pull origin main
            docker-compose pull
            docker-compose up -d --build
          EOF
